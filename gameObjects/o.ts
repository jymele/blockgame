import { config } from "@/utils/config";

export const oPiece: number[][] = [
  [0, 1, config.noOfCols, config.noOfCols + 1],
  // [0, 1, config.noOfCols, config.noOfCols + 1],
  // [0, 1, config.noOfCols, config.noOfCols + 1],
  // [0, 1, config.noOfCols, config.noOfCols + 1],
];
